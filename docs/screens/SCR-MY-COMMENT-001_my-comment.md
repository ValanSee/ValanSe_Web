# 화면 명세서: 내 댓글 (My Comments)

## 📌 기본 정보

| 항목 | 내용 |
|---|---|
| 화면 ID | SCR-MY-COMMENT-001 |
| 화면명 | 내 댓글 |
| 경로 | `/my/comment` |
| 인증 필요 | ✓ |
| 작성일 | 2026-05-16 |

## 🎯 화면 목적

내가 작성한 댓글 목록을 조회하고, 다중 선택해 일괄 삭제한다.

## 🚪 진입 경로

- 내 정보(`/my`) → "내 댓글"

## 📐 레이아웃 구성

1. **헤더**
2. **정렬 옵션** (최신순 / 오래된순)
3. **댓글 리스트** (각 항목: 체크박스 + 댓글 본문 + 원본 게임 메타)
4. **하단 일괄 삭제 버튼** (1개 이상 선택 시 활성화)

## 🧩 섹션별 상세 명세

### 1. 정렬 옵션

**사용자 액션**: 최신/오래된 토글 → 리스트 재정렬

### 2. 댓글 리스트

**표시 데이터**

| 항목 | 출처 | 비고 |
|---|---|---|
| 댓글 본문 | `fetchMyComments` | - |
| 작성일 | 동일 | - |
| 원본 게임 정보 | 동일 | 클릭 시 `/poll/[id]` |

**사용자 액션**

- 체크박스 선택
- 본문 클릭 → 원본 게임으로 이동 (TODO: 확인)

### 3. 일괄 삭제

**사용자 액션**: 삭제 버튼 → 확인 모달 → `deleteMyComments`

## 🔄 사용자 시나리오

**시나리오 1: 여러 댓글 일괄 삭제**

1. 정렬 변경
2. 댓글 N개 선택
3. 삭제 버튼 클릭 → 확인 모달 → 삭제
4. 리스트 갱신

## ⚠️ 예외 상황

| 상황 | 처리 |
|---|---|
| 댓글 없음 | 빈 상태 안내 |
| 삭제 실패 | 토스트 + 선택 상태 유지 |

## 🔌 사용 API

| Method | Endpoint | 용도 |
|---|---|---|
| GET | `/comments/mine?sort&size=50` | 내 댓글 목록 (응답 키 `comments`) |
| DELETE | `/comments/{commentId}` | 댓글 삭제 (다중 선택 시 병렬 호출) |
> **응답 형태 과도기**: 서버가 목록 응답을 배열 → `{ <목록키>, page, size, hasNext }` 엔벨로프로 전환 중이다(서버 `dev` 적용 / `main` 미적용). 프론트는 `unwrapList`로 두 형태를 모두 배열로 정규화하며, 페이지네이션 UI가 없으므로 `size=50`(서버 상한)을 요청해 기존 동작을 유지한다.


## 📎 관련 문서

- `SCR-MY-001_my.md`
- `SCR-POLL-001_poll-detail.md`
